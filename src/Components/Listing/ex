const [selOption, setselOption] = useState()
    const [selData, setselData] = useState([])


    useEffect(() => {
        const arr = selData;
        arr.push(selOption);
        setselData(arr);
        console.log(selData);
        console.log(selOption);   
    }, [selOption])